<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', Rails.env.test? ? confirmation_url(@resource, confirmation_token: @token) : "#{ENV['CONFIRMATION_EMAIL_URL']}#{@token}" %></p>